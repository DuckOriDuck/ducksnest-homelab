apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: databases
  annotations:
     tailscale.com/expose: "true"
     tailscale.com/hostname: "ducks-db"
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
